[core]
    excludesfile = ~/.gitignore
	  editor = nvim
[user]
	  name = MatteoScorcia
    email = matteoscorcia@gmail.com
  # Enforce SSH
[url "ssh://git@github.com/"]
    insteadOf = https://github.com/
[color]
    status = auto
    diff = auto
    branch = auto
    interactive = auto
    grep = auto
    ui = auto
